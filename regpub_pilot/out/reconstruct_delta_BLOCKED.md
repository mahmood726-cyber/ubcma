# Reconstruct-Delta — BLOCKED (honest partial)

**Question:** How many percentage points do R2 (2×2 recovery / effect-from-counts) +
R3 (SD/SE imputation) add to the **33.7%** poolable anchor on the fixed N=600
completed-T2D corpus (`out/metrics.json` `poolability.poolable_pct`)?

**Verdict: delta BLOCKED. RECOVERED = 0 from R2/R3 ingredients; RIGHT uncheckable (no per-trial gold).**
Script: `src/reconstruct_delta.py` → `out/reconstruct_delta.json` (reproducible, model-free).

---

## Three claims, held separate (never collapsed)

| Claim | Count | Tag |
|---|---:|---|
| **FOUND** (type-eligible R2/R3 **AND** state `NEITHER` = currently not poolable) | **127** (R2 101, R3 26) | VERIFIED |
| **RECOVERED** (poolable effect + uncertainty produced from R2/R3 ingredients) | **0** (R2 0, R3 0) | VERIFIED |
| **RIGHT** (matches an independent per-trial gold within tolerance) | **0** | BLOCKED — no gold |
| **Realised Δ (pp)** | **— BLOCKED —** | BLOCKED |

The type-eligible gross ceiling of **160** (relative_only 99 + numbers_no_estimate 35 +
pvalue_only 15 + median_no_dispersion 11) **must not** be reported as an achievement: 33 of
those 160 are already state `ONE`/`BOTH` (already inside the 33.7% via registry or abstract),
so they contribute **zero** delta by construction. Only the **127** `NEITHER` subset can move
the anchor — and none did.

## Why RECOVERED = 0 (ingredients absent at source, not merely unextracted)

| Method / reason | n | Root cause |
|---|---:|---|
| R2 `no_registry_resultsSection` | 100 | Trial posts **no CT.gov results** → no 2×2 event counts, no per-arm denominators anywhere. Abstract is relative-only/numbers-no-estimate → no counts either. Nowhere to build a 2×2. |
| R2 `no_two_arm_event_counts` | 1 | Has a results section but the single primary is a `NUMBER`-type continuous measure, not a per-arm binary count. |
| R3 `pvalue_only_has_no_point_estimate` | 18 | SE = \|effect\|/z requires a **point estimate** as numerator; by definition `pvalue_only` has none. p alone (often an inequality `p<0.05`) yields neither the effect nor an exact z. |
| R3 `median_no_IQR_or_range_present` | 5 | No dispersion token at all → Wan 2014 / Luo 2018 inapplicable (dispersion is the required input). |
| R3 `median_dispersion_not_arm_paired_outcome` | 3 | An IQR/range token exists but (manually audited) the median is a **baseline / PK / degenerate feasibility** value (e.g. baseline UACR; days-quantifiable-in-plasma; Likert 5.0 IQR 5.0–5.0), **not** the two-arm treatment effect. Applying Wan/Luo would fabricate a garbage effect. |

**Structural reason this is not a tooling gap:** a trial lands in state `NEITHER` *precisely
because* neither open source carries a poolable number — the registry posted no results
(100/101 R2-eligible have no `resultsSection`; 25/26 R3-eligible likewise) and the abstract is
relative-only / p-only / median-without-dispersion. R2's counts and R3's numerators are
**absent at origin**. Statistical reconstruction cannot synthesise data that exists in no
accessible source.

## NAMED BLOCKER (what must be wired to complete the RIGHT check + a real delta)

1. **Per-trial gold-effect file** — none exists (searched `out/` for `gold|groundtruth|answer|truth`: 0 hits).
   To check RIGHT you need an independent map `NCT → verified published primary effect + CI`
   for these 127 trials. Their effect is by definition **not** in registry results (none posted)
   nor in the index abstract (unusable). Gold would have to come from the **full-text
   methods/results**, which the mission's open-data constraint (abstract + registry only) does
   not supply, and no such file is present in the repo.
2. **A source of R2 counts / R3 dispersion** — same origin problem. Absent an external
   effects table, the 100 no-results trials remain unreconstructable by any method.

Without (1), **fabricating a delta is exactly the retracted failure mode** (`SEPTUM-ADVERSARIAL`,
`CO2-EXCRETION` §0). Reported honestly: **R2/R3 add 0 gold-checked pp; the realised delta is BLOCKED.**

## Adjacent context (a DIFFERENT method, not this delta)

The open-access full-text JATS layer (`out/fulltext_enriched_t2d.jsonl`, still open-access,
mission-compatible) produced a *verified* effect for **11 of the 127** `NEITHER` trials — but
that is **full-text extraction**, not R2/R3 statistical reconstruction from counts, and its
RIGHT-vs-gold status is likewise unchecked (the extracted number would *be* the recovered
value, so it cannot also serve as its own independent gold). It is noted only to show where a
real recovery lever for this subset actually lives (full text), consistent with the pilot's
own finding that abstract+registry alone cannot reconstruct these.

---
*Generated by `src/reconstruct_delta.py`. No merge/push/commit; new files only. Data read-only.*
